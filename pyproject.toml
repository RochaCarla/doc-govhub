[tool.poetry]
name = "govhub-docs"
version = "0.1.0"
description = "Documentação técnica do GovHub BR"
authors = ["GovHub BR <lablivreunb@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.11"
mkdocs = "^1.6.0"
mkdocs-material = "^9.6.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
